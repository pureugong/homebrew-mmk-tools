# homebrew-mmk-tools

Homebrew tap for MMK tools.

- Tap: `brew tap pureugong/mmk-tools`
- Install: `brew install mmk-memory`

`mmk-memory` is released from a private repo, so installs require:

```bash
export HOMEBREW_GITHUB_API_TOKEN=...
```
